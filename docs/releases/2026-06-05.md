# Release Notes: 2026-06-05

## `2026.6.5`

- Published release identity `v2026-06-05` and package version
  `supply-chain-monkey==2026.6.5`.
- Kept the Python import package as `scm` for client and server code.
- Added PyPI Trusted Publishing workflow scaffolding for the `pypi`
  GitHub environment.
- Documented the repo's two supported surfaces: `scm.client` for consumers and
  `scm.server` for the deployed Appliku service.
