# Release Notes: 2026-06-16

## `2026.6.16`

- Published `supply-chain-monkey==2026.6.16` with release identity
  `v2026-06-16`; the Python import package remains `scm`.
- Added exact supplier part number lookup endpoints:
  `GET /v1/spn` and `POST /v1/spn/batch`.
- Exposed provider capability and rate-limit metadata for client and GUI tools.
- Updated JLCPCB MPN search to use the current public component-search endpoint
  with legacy scraper fallback.
- Updated LCSC search for the current `/ftps/wm/search/v3/global` response
  shape.
- Added and validated live supplier coverage for JLCPCB, LCSC, Digikey, and
  Mouser.
