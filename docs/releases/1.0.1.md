# Release Notes: 1.0.1

## `1.0.1`

- Added `wn-dev-std` baseline files, docs, and Rack signoff.
- Added Appliku deployment contract tests for the managed uv build path.
- Moved FastAPI, Uvicorn, and Requests into base runtime dependencies.
- Removed executable references to retired `supply_chain_monkey` compatibility
  imports and provider-level `.env` loading.
