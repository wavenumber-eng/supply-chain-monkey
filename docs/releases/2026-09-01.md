# Supply Chain Monkey Release Notes: 2026-09-01

## `2026.9.1`

- Publish `supply-chain-monkey==2026.9.1` with release identity
  `v2026-09-01`; the Python import package and Rust proof CLI remain `scm`.
- Establish TypeSpec as the structural authority for generated OpenAPI, JSON
  Schema, Python, catalog, and Rust contract projections.
- Add the asynchronous Rust client and `scm` CLI with generic multi-provider
  search, JSON output, and a concise terminal table.
- Restore JLCPCB and LCSC generic search and preserve supplier identity and
  result limits through their fallback pipelines.
- Expose local and deployed Swagger/ReDoc explorers for both the runtime and
  canonical TypeSpec OpenAPI documents, backed by documentation and parity
  tests.
