# Release Notes: 2026-08-12

## `2026.8.12`

- Publish `supply-chain-monkey==2026.8.12` with release identity
  `v2026-08-12`; the Python import package remains `scm`.
- Preserve DigiKey OAuth and Product API failures as `provider_error` instead
  of reporting false `not_found` responses.
- Add sanitized structured failure details, including retryability and
  upstream HTTP/request context, to service envelopes and the demo/status
  page.
- Emit Core Metadata 2.4 for compatibility with the release upload toolchain.
