+++
type = "plan_log"
id = "alexandria-consumer-proof-close"
plan_id = "scm-rust-client-typespec"
step_id = "alexandria-consumer-proof"
created = "2026-08-31T20:19:24-04:00"
+++

# Log: alexandria-consumer-proof

# Log: close scoped Alexandria consumer proof

The retained `scm-client` and `scm-contracts` candidate bytes were extracted
and consumed through the path-free standalone Alexandria Rust broker shape.
Its committed manifest names `scm-client = "=0.1.0"`; the proof harness applies
only temporary crates.io patches to the extracted content-addressed candidates.
No permanent sibling or machine path is present.

The broker privately retains SCM configuration, preserves SCM-owned
`MultiSearchResults`, `ProviderOutcome`, and `ClientError` classifications, and
adds only application cancellation control. Five tests prove missing
configuration, unreachable service, mixed success/provider error, already
signalled and in-flight cancellation, bounded scheduling, successful
multi-provider search, and authorization-header-only token transport. The
reviewer independently repeated locked Clippy with warnings denied and all
tests against the retained archive bytes; 5/5 passed with no P0-P2 findings.

This closes the exact-candidate, non-production consumer proof. It does not add
an Alexandria HTTP route, browser DTO, environment variable, provider adapter,
or credential file, and it does not transfer supplier ownership from SCM.
Normal Alexandria workspace integration remains separately gated by
Alexandria's active plan and by an immutable Git or registry dependency source;
the temporary proof patch must never become a deployed path dependency.
