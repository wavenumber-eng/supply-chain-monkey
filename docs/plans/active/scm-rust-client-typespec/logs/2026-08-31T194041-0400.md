+++
type = "plan_log"
id = "rust-l99-and-ci-integration"
plan_id = "scm-rust-client-typespec"
step_id = "rust-standard-adoption"
created = "2026-08-31T19:40:41-04:00"
+++

# Log: Rust L99 and CI integration

Closed the execution gap between the Rust sidecar policy manifest and actual
Rack behavior. Root L99 now discovers an executable Rust subtest covering the
exact pinned wn-dev-std 2026.8.12 repo/language audit, deterministic codegen,
format, locked check, Clippy with warnings denied, workspace tests, doctests,
and rustdoc with warnings denied. The sidecar `rust/tests/rack.toml` remains
because the pinned standard audits its declared command surface; the root
pytest subtest is what makes those gates executable by Rack.

CI now repeats the locked Rust signoff on Windows, macOS, and Linux, including
the in-process TLS, private-root, proxy, redirect, timeout, response-bound, and
credential-safety tests contained in the workspace suite. The Python/Appliku
signoff job is unchanged except that its normal L99 discovery now includes the
Rust gate. Fresh local execution reports eight passing Rust L99 tests; workflow
YAML parses and Rack lists the new subtest.
