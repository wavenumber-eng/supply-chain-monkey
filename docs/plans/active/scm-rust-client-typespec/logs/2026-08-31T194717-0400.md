+++
type = "plan_log"
id = "cross-language-conformance-slice"
plan_id = "scm-rust-client-typespec"
step_id = "cross-language-conformance"
created = "2026-08-31T19:47:17-04:00"
+++

# Log: cross-language conformance

Python and Rust codecs and clients already consumed the same digest-bound
manifest and endpoint vectors. A focused default/nullability audit found that
the Rust typify projection unnecessarily removed JSON Schema defaults, leaving
an omitted `SpnBatchRequest.include_raw` as `None` while Python materialized the
TypeSpec default `false`. Retaining schema defaults makes the generated Rust
field a serde-defaulted bool and keeps the Rust client request construction
source aligned with the generated type.

Added shared valid omission and invalid explicit-null vectors, rebound the
catalog digest, and regenerated packaged Python and Rust resources. Both
language codecs now run the same omission/null vectors; a Rust assertion proves
the native false default while the schema gate rejects null before native
decode. Contract, vector, Python, and Rust generation freshness passes; 36
focused Python conformance tests and the complete locked Rust workspace tests
pass with Clippy warnings denied. The slice remains active pending independent
review.
