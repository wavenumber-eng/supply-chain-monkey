+++
type = "plan_log"
id = "scm-rust-client-typespec-2026-08-31T201404-0400"
plan_id = "scm-rust-client-typespec"
step_id = "artifact-candidate-proof"
created = "2026-08-31T20:14:04-04:00"
+++

# Log: artifact-candidate-proof

# Log: retain and verify exact artifact candidates

Independent review rejected the earlier hash-only evidence because the harness
deleted every candidate and identical-source rebuilds did not reproduce the
archive hashes. The hashes in `2026-08-31T195404-0400.md` are therefore
superseded and must not be used as retrievable candidate identities.

Commit `0b1edb9a5ec2578ab1b095469509a2d0d62d8930` remediates the proof. The
harness now retains every candidate beneath a content-addressed
`temp/artifact-candidates/sha256/<digest>/` path and atomically writes the
machine-readable `temp/artifact-candidates/latest.json` manifest. The clean
proof manifest records that exact commit with `dirty: false`.

The same run:

- scrubbed Python import-control variables, used `python -I`, and asserted the
  imported `scm` package and resources resolve inside the clean Python 3.13
  environment;
- checked the exact catalog-derived schema filename and digest inventory in
  the wheel, sdist, and extracted `scm-contracts` archive;
- rejected any Cargo `target` tree in the sdist;
- extracted all Rust candidates into an isolated directory;
- copied the path-free Alexandria broker consumer manifest, patched only its
  crates.io sources to those extracted candidate bytes, and used a fresh Cargo
  target with Rust 1.96.1; and
- passed locked downstream Clippy with warnings denied plus all five consumer
  tests and doctests.

Retained clean-candidate identities:

- wheel: `c2735559c1c82dec08741b1b7fe97ca3bc62e584a102cbb6f7303b2b49c78f76`
- sdist: `b00acceed7932b6e335db238b659098a302839f60e59a65192594d8d68d95877`
- `scm-contracts`: `fbc605b2d2eaa52fab0e5c49cfb0479bab4ee1b3a936cc0bb447b5224eb40ea5`
- `scm-client`: `ea3b12d352cf7c55f6e8a4ba44ac23fc7d28591bd69226411a5d74cff7ffe042`
- `scm-cli`: `f50a48b788987523938dc74cfa16fa9607dcdd60a1ad355cabb01459ef0b0310`

No crate, wheel, Git ref, or deployment was published. The retained staging
area is local release-candidate evidence, not a public distribution channel.
The artifact step remains active pending independent rereview.
