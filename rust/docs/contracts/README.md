# Contracts

SCM v1 Rust models are generated from `../../../contracts/scm/v1/generated`
through the machine-readable catalog. Runtime validation uses the unmodified
JSON Schemas, not a generator projection.
