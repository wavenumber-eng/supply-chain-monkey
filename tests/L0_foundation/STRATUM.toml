name = "Foundation"
order = 0
description = "Supply-chain package bootstrap, env loading, and opt-in provider validation"
enabled = true
concerns = ["core", "env", "providers"]

objectives = [
    "Expose a stable supply_chain_monkey package/import surface",
    "Keep .env handling local to the package instead of relying on the monolith settings layer",
    "Make live provider tests explicit opt-in rather than default network traffic",
]
