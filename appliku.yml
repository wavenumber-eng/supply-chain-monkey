build_settings:
  build_image: dockerfile
  dockerfile_path: code/Dockerfile
  dockerfile_context_path: code
  container_port: 8000

services:
  web:
    command: uvicorn supply_chain_monkey.main:app --host 0.0.0.0 --port 8000
